#include <bits/stdc++.h>
using namespace std;
void print()
{
    cout << "pranav kumar" << endl;
    ;
}
int sum(int a, int b)
{
    return a + b;
}
int main()
{
    // so basically we  have to learn algorithms and containers and functions and iterators in the stl ;
    print(); // this bits/stdc++ function helps us to get all the variables to run and make sure and all not instead of other variables
    cout << sum(5, 6);
    // using namespace std helps us to get rid of the parts like  std:: cout<<a and all
    return 0;
}