#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> mm;
    for (int i = 0; i < 5; i++)
    {
        mm.push_back(i);
    }
    for (int j = 0; j < mm.size(); j++)
    {
        cout << mm[j] << " ";
    }
    return 0;
}