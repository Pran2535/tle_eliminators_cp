#include <iostream>
#include <vector>
using namespace std;

int main()
{
    vector<int> mm;
    for (int i = 0; i < 6; i++)
    {
        int temp;
        cin >> temp;
        mm.push_back(temp);
    }

    return 0;
}